node_modules
dist
src-tauri/target
.env
.claude/
*.log
# Regenerated byte-identical to the tracked schemas by the Windows cross-check
# (cargo check --target x86_64-pc-windows-gnu); tracking it would duplicate 414KB.
src-tauri/gen/schemas/windows-schema.json
